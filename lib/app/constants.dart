abstract final class Constants {
  static const double searchBarHeight = 70.0;
  static const double searchButtonSize = 48.0;
  static const double searchIconSize = 30.0;
  static const double searchHorizontalPadding =
      (searchBarHeight - searchButtonSize) / 2.0;
  static const double searchButtonPadding = searchHorizontalPadding / 2.0;
}
